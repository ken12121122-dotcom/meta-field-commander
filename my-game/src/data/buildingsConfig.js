/**
 * 建築設定
 *
 * 定義每個場域包含的建築，以及各建築的升級效果。將原型
 * 中 `buildingCatalog` 與升級資料搬移至此檔案。
 */

export const buildingCatalog = {
  // e.g. personal: [{ id: 'sleepPod', name: '睡眠艙', maxLevel: 5, effect: (level) => level * 2 }],
};