/**
 * 角色設定
 *
 * 包含遊戲中所有角色的名稱、描述、信任值等設定，以及對應的
 * 劇情事件。依照 v0.5 規格將實際資料填入此檔案。
 */

export const characters = {
  // e.g. lixn: { name: '璃歆', role: '女王型教練', initialTrust: 50 },
};

export const storyEvents = [
  // e.g. { id: 'event1', character: 'lixn', description: '...', effect: (choice) => { /* ... */ } },
];