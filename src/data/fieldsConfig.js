/**
 * 場域資料設定
 *
 * 用來描述每個場域的名稱、坐標與可建築模組。依據 v1.4 規格的
 * `fieldMap` 與 `domains` 結構，將實際資料填入此檔案。
 */

export const domainMap = {
  // e.g. personal: { label: '個人房間', position: { left: '10%', top: '20%' } },
};

export const domains = [
  // e.g. { id: 'personal', label: '個人房間' },
];